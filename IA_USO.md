1.- Al insertar mi proyecto de Java que contenia el inicio de sesion para la base de datos me salia el error de que no encontraba un driver que pudiera conectarse a la base de datos MariaDB, aun despues de haber insertado el driver que usaba en IntelliJ, por lo que le pregunte a la IA de Visual Sudio Code por ayuda con la siguiete prompt:
"Tengo un archivo de Java el cual contiene una validacion de credenciales para un inicio de sesion, pero al intentar correrlo en la terminal me salie un error de driver no encontrado, incluso despuesde haber insertado uno, me podrias explicar paso a paso como puedo resolver este problema?"
La IA creo 2 archvios llamados .classpath y .project que ayudaban con la configuracion del driver y hacian que Visual pudiera aceptar mi driver, luego me dijo que presionara Ctrl + Shift + P y buscara Java: Clean Java Language Server Workspace en la ventana que aparecia para limpiar el cache y reiniciar desde 0 el intento de conexion, el cual funciono.

2.- En el archivo de InicioSesin.HTML es necesario llevar al usuario a la pestaña necesaria, ya sea Dueño o Empleado, por lo que se uso a la IA de Visual Studio para apoyar en la localizacion de las variables que habia que cambiar para hacer eso, usando la prompt "I have another issue, I need to make sure the page leads to the correct site whenever I input a certain user based on two tables inside my data base, Usuarios and Roles, in the first one I my data is labeled "Nombre_Compledo", "Contraseña", "ID_Roles" and the second one has "Id_roles" and "Nombre_Rol", what I need you to do is help me make sure that whenever a 1 is read on the first table, you lead me to Dueno.html, and when you get a 2 you lead me to Empleado.html" Sabiendo esto, la IA cambio 2 lienas en el archivo de Java que comparaban el nombre y ahora comparan el Id del rol, luego en el archivo de InicioSesin.html se cambiaron 3 lineas para hacer esto mismo y asi dejarme moverme entre interfaces

3.-Para el archivo de Seguridad.html tuvimos que hacer uso de la IA de Visual nuevamente para ayudarnos con el metodo que guarda la informacion que insertamos para registrar un empleado, le preguntamos usando "We are having trobule withthe code thats supposed to save the data we insert on Seguridad.html, can you give us a brief rundown as to how we should do it?" y "Ok, the data is saved correctly and the code is correctly tryingo to insert it to the database, but we keep getting an error thats telling us that the data is incomplete, can you take a look at it?" asi nos dieron el metodo de insercion en el archivo de conexion y en el de seguridad

4.- En ese mismo archivo nosotros quisimos hacer que la tabla fuera una tabla que desplegara los datos de los empleados con su contraseña y rol, pero al tratar de realizar el codigo nos funciono bien con los campos de Nombre y Cargo pero la contraseña salia undefined, ais que le preguntamos a la IA "Hey, so we tried to replace the original table with anew one that showed the actual data straight from the database and it did work for the name and role, but the password appears to be undefined, and we don't really know what's going on, can you help us?" y ya al analizar el codigo se dio cuenta que el Javascript no estaba reconociendo la Ñ y en sulugar salia el simbolo de que el caracter no existe (�), por lo que nos ayudo a cambiarlo a un alias que pudiera darnos la contraseña